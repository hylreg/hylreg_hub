[project]
name = "hylreg-hub"
version = "0.1.0"
description = "在一个仓库中管理多个 Git 仓库"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
manage-repos = "scripts.manage_repos:main"

